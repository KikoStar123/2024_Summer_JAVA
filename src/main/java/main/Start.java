package main;

/**
 * Start 类是应用程序的启动入口类。
 * 它的作用是调用 MainClass 的 main 方法来启动整个应用程序。
 */
public class Start {

    /**
     * 应用程序的主入口，调用 MainClass 的 main 方法以启动应用程序。
     *
     * @param args 命令行参数
     */
    public static void main(String[] args) {
        MainClass.main(args);
    }
}
